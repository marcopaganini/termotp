# yaml-language-server: $schema=https://goreleaser.com/static/schema.json
# vim: set ts=2 sw=2 tw=0 fo=cnqoj

version: 2

before:
  hooks:
    # You may remove this if you don't use go modules.
    - go mod tidy
    # you may remove this if you don't need go generate
    - go generate ./...

builds:
  - env:
      - CGO_ENABLED=0
    goos:
      - linux
      - windows
      - darwin
    ldflags:
      - -s -w -X main.Build={{.Version}}

archives:
  - format: tar.gz
    files:
      - LICENSE
      - README.md
    # use zip for windows archives
    format_overrides:
      - goos: windows
        format: zip

nfpms:
  - homepage: https://github.com/marcopaganini/termotp
    maintainer: Marco Paganini <paganini@paganini.net>
    description: |-
      termotp - A terminal OTP codes generator.
    license: MIT
    formats:
      - apk
      - deb
      - rpm
      - archlinux
    provides:
      - termotp
    bindir: /usr/bin
    contents:
      - src: README.md
        dst: /usr/share/doc/termotp/README.md
      - src: LICENSE
        dst: /usr/share/doc/termotp/LICENSE.txt

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
